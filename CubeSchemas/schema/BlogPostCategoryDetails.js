cube(`BlogPostCategoryDetails`, {
  sql: `SELECT * FROM ebdb.blog_post_category_details`,
  
  preAggregations: {
    // Pre-Aggregations definitions go here
    // Learn more here: https://cube.dev/docs/caching/pre-aggregations/getting-started
  },
  
  joins: {
    BlogPostDetails: {
      sql: `${CUBE}.blog_post_detail_id = ${BlogPostDetails}.id`,
      relationship: `belongsTo`
    },
    
    BlogCategoryMasters: {
      sql: `${CUBE}.blog_category_master_id = ${BlogCategoryMasters}.id`,
      relationship: `belongsTo`
    }
  },
  
  measures: {
    count: {
      type: `count`,
      drillMembers: [id, createdAt, updatedAt]
    }
  },
  
  dimensions: {
    id: {
      sql: `id`,
      type: `number`,
      primaryKey: true
    },
    
    createdAt: {
      sql: `created_at`,
      type: `time`
    },
    
    updatedAt: {
      sql: `updated_at`,
      type: `time`
    }
  },
  
  dataSource: `default`
});
