cube(`BlogPostTagDetails`, {
  sql: `SELECT * FROM ebdb.blog_post_tag_details`,
  
  preAggregations: {
    // Pre-Aggregations definitions go here
    // Learn more here: https://cube.dev/docs/caching/pre-aggregations/getting-started
  },
  
  joins: {
    BlogPostDetails: {
      sql: `${CUBE}.blog_post_detail_id = ${BlogPostDetails}.id`,
      relationship: `belongsTo`
    },
    
    BlogTagMasters: {
      sql: `${CUBE}.blog_tag_master_id = ${BlogTagMasters}.id`,
      relationship: `belongsTo`
    }
  },
  
  measures: {
    count: {
      type: `count`,
      drillMembers: [id, createdAt, updatedAt]
    }
  },
  
  dimensions: {
    id: {
      sql: `id`,
      type: `number`,
      primaryKey: true
    },
    
    createdAt: {
      sql: `created_at`,
      type: `time`
    },
    
    updatedAt: {
      sql: `updated_at`,
      type: `time`
    }
  },
  
  dataSource: `default`
});
